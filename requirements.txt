# Runtime dependencies for the HuggingFace Space.
# For local development install the package instead:  pip install -e ".[dev,eval]"

# Retrieval stack
sentence-transformers>=3.0,<4.0
transformers>=4.40,<4.46
tokenizers>=0.19,<0.21
faiss-cpu>=1.7.4
rank-bm25>=0.2.2
numpy>=1.24,<2

# Config
pydantic>=2.0
pydantic-settings>=2.0

# LLM / GPU (ZeroGPU)
torch>=2.0
accelerate>=0.27
spaces

# UI + document parsing
gradio==4.44.0
# Pin the web stack to what gradio 4.44.0 was built against. starlette 0.41+
# changed TemplateResponse's signature, which makes gradio 4.44.0 crash with
# "TypeError: unhashable type: 'dict'" when serving the page.
fastapi==0.112.2
starlette==0.38.6
pypdf>=5.0
